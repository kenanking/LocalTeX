# parity-base receipts

Trunk baseline for the Zed v1.17.2 GPUI pin.

`size.txt` is the byte size of `target/dev-opt/localtex` after `cargo build --profile dev-opt`.

PNG slugs match the live lanes in `docs/superpowers/plans/2026-09-01-gpui-v1172-migrate.md`.

Regenerate with `./scripts/parity-capture.sh`.
